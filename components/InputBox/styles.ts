import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  input: {
    width: "100%",
    height: 50,
    padding: 15,
    borderRadius: 15,
    borderWidth: 2,
    borderColor: "#ddd",
  },
  text: {
    fontSize: 15,
    fontWeight: "500",
    color: "#2705FA",
    marginBottom: 5
  }
});